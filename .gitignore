__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Market data cache and backtest output are reproducible, not source
data/cache/
out/

# data/exchange/ is deliberately NOT ignored: it is how candles reach a
# container that cannot call an exchange itself.

# Never commit credentials
.env
*.key
secrets.yaml

# Suchläufe schreiben ihre Rohtabellen hierher; sie sind reproduzierbar.
research/search_*.csv
